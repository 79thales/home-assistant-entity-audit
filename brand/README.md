# Entity Audit brand assets

- `icon.png`: 256 × 256 px
- `icon@2x.png`: 512 × 512 px

Both files use a transparent background. Runtime copies are included under `custom_components/entity_audit/brand`, which Home Assistant 2026.3 and newer can load directly for custom integrations.
